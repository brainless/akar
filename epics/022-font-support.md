# Epic 022: Font Support

**Status:** In Progress — Tasks 1-7 Done, Tasks 8-9 Not Started. Task 7 landed on 2026-08-14: `FontFamily::Named(u32)` selects a runtime-loaded family, resolved inside `akar-core` through the new owned `FontRequest`/`FontSelection` request and `TextPipeline::set_text_styled` (the three generic-family component call sites migrated to it too), and the C ABI gained `akar_load_font_bytes` with distinct `AKAR_FONT_LOAD_*` codes plus an `AkarTextStyle::font_family_name_handle` field on a `UINT32_MAX` sentinel. Tasks 5 and 6 landed together earlier the same day: `akar-core` now owns `FontSource`/`FontLoadError`/`TextPipelineConfig`, builds its `fontdb::Database` explicitly (no implicit system scan), loads fonts from bytes through `TextPipeline::load_font_bytes` under the single-family rule, and bundles IBM Plex Sans Regular + SemiBold behind a default-on `bundled-font` feature so default rendering is reproducible out of the box. Still open: the `AkarFontSource` parameter on `akar_ctx_new`/`akar_ctx_mock` (Task 8, which is why those two keep their current signatures and use the default config today), and the CJK/Arabic prototype (Task 9). Historical context below. Tasks 1-4 (research) are Done and were re-verified on 2026-08-12 against current source. A second readiness audit found two implementation-blocking API issues in Tasks 5 and 7 (see "Readiness audit — 2026-08-12"); both were resolved as design decisions on 2026-08-14 (see "Decisions — 2026-08-14"): v1 restricts font loading to single-family sources (error on collections with more than one distinct family), and named-family selection resolves through an owned request passed into a new `TextPipeline` entry point rather than a borrowed registry name. Two further decisions were sealed on 2026-08-14 (same section, items 3 and 4): the bundle expands to IBM Plex Sans Regular + SemiBold because a single face would render `FontWeight::Bold` at regular weight, and the font assets are committed to the repo behind a default-on `bundled-font` Cargo feature rather than gitignored (`include_bytes!` resolves at compile time, and an empty font database panics). (At the time those decisions were written, Tasks 5 and 7 were unblocked, Tasks 6, 8 and 9 dependency-blocked in sequence, and no implementation code existed; Tasks 5 and 6 have since landed, as recorded above.)
**Goal:** Give applications control over which fonts akar loads and falls back to, so text renders correctly for scripts beyond the current default font's coverage (CJK, Arabic, Devanagari, emoji, etc.).

**Prerequisite:** Epic 021 is `Status: Done`.

---

## Introduction

akar's text pipeline wraps glyphon, which is backed by cosmic-text and, beneath that, `fontdb` for font matching and a HarfBuzz-derived shaper for shaping. (Correction, 2026-08-12: the pinned cosmic-text 0.18.2 shapes with `harfrust` and rasterizes via `swash`/`skrifa`; `rustybuzz` is not a dependency at all — it does not appear in `Cargo.lock`. See `Cargo.lock:518-534` for cosmic-text's actual dependency list.) Font matching and fallback are not novel problems — they are largely solved inside cosmic-text already. What is unclear is how much of that capability akar currently exposes to applications versus hard-codes internally.

Epic 020 introduced akar-owned typography types (`AkarTypography` and friends) so that component styling APIs do not leak glyphon types across the C ABI. Font support needs to extend that same boundary: applications must be able to load custom font files/bytes and configure fallback without ever touching `fontdb` or `cosmic_text` directly, including from non-Rust languages through `akar.h`.

This epic is investigation-first. The tasks below are for a coding agent to establish the current state of font handling in `akar-core`'s `TextPipeline` before any API is designed.

---

## Research

Initial inputs (pre-investigation hypotheses, kept for context):

- **cosmic-text/fontdb already do the hard part.** Font matching, family fallback chains, and system font discovery are `fontdb` features. akar likely does not need to reimplement matching logic — it needs an API surface that configures `fontdb`'s font source (bundled bytes vs. system scan) and exposes family/fallback selection to callers.
- **Local source of truth**: `~/Projects/glyphon` for how akar's `TextPipeline` currently constructs its `FontSystem`, and whether system font scanning is enabled today (this has portability and cold-start-time implications — system font scanning is slow and non-deterministic across machines, which cuts against akar's "identical screenshot on macOS/Windows/Linux" design goal in `DEVELOP.md`).
- **Bundled vs. system fonts is a real tradeoff.** Bundled fonts guarantee reproducible screenshots (important for the agent-driven visual feedback loop and `akar-diff` regression gating) but grow binary size and require akar to ship/redistribute font files with a license story. System fonts are free but make `demo-rust` screenshots non-reproducible across machines, which directly conflicts with the screenshot-based debug toolchain this project depends on.
- **C ABI shape matters early.** Loading a font from bytes (`akar_load_font_bytes(ctx, bytes, len)`) is straightforward to bind from any language. Anything that leans on filesystem paths or platform font APIs is harder to keep portable and should be treated with caution.
- **Fallback chains are string-directed today in most cosmic-text-based apps** — an ordered list of family names with a terminal fallback. Whatever akar exposes should probably mirror that rather than inventing a new configuration model.
- **This epic is a soft prerequisite for [[023]] and [[024]]** — RTL and CJK/complex-script rendering are meaningless without the right fonts loaded and falling back correctly.

### Confirmed findings (this investigation)

> Re-verified 2026-08-12. Read this section together with "Review — 2026-08-12 verification pass" below, which corrects several line numbers and field names and supersedes any conflicting statement. The dependency sources are also available already-extracted at `~/.cargo/registry/src/index.crates.io-*/{cosmic-text-0.18.2,fontdb-0.23.0}/` — no need to unpack `.crate` archives.

All findings below are from reading the actual pinned dependency source (extracted from the cached `.crate` archives at `~/.cargo/registry/cache/index.crates.io-1949cf8c6b5b557f/{cosmic-text-0.18.2,fontdb-0.23.0}.crate`, since neither crate has a `~/Projects/` checkout — only `glyphon` does, and glyphon itself does no font-system work; see below) plus akar's own source. `cargo check --workspace` was run read-only and succeeds against these exact pinned versions (see `Cargo.lock` lines 514-517 `cosmic-text 0.18.2` name/version/checksum, dependency list continuing to line 533; 725-728 `fontdb 0.23.0` name/version/checksum, dependency list continuing to line 733), confirming the source read matches what's actually compiled into akar today.

1. **`glyphon` does not own font loading at all — it is a thin wgpu-rendering wrapper.** `~/Projects/glyphon/src/lib.rs:23-31` re-exports `cosmic_text`'s public API wholesale, including `fontdb` itself (`pub use cosmic_text::{self, fontdb, ..., FontSystem, ...}`). `glyphon::TextRenderer`/`TextAtlas` (`~/Projects/glyphon/src/text_render.rs`, `text_atlas.rs`) take `&mut FontSystem` as a parameter to `prepare()`/rasterization calls but never construct one. All font loading, matching, and fallback logic lives in `cosmic-text`, and beneath it, `fontdb`. This confirms the epic's hypothesis: akar does not need to reimplement font matching, only decide how `FontSystem`/`fontdb::Database` get populated and exposed.

2. **akar constructs exactly one `FontSystem`, via the zero-config constructor, with no override point.** `crates/akar-core/src/text_pipeline.rs:41` — `TextPipeline::new()` calls `glyphon::FontSystem::new()` unconditionally. The only other call site is a test helper at `text_pipeline.rs:386` (same pattern). There is no parameter, builder, env var, or feature flag anywhere in `akar-core`, `akar-components`, or `akar-c-api` that changes this. `AkarCore::new` (`crates/akar-core/src/context.rs:24-42`) calls `TextPipeline::new(device, queue, surface_format)` directly at line 35, and `akar_ctx_new` (`crates/akar-c-api/src/lib.rs:368-395`) calls `AkarCore::new` at line 384 with no font-related parameters. There is no bytes-loading, path-loading, or fallback-chain API today, in Rust or C.

3. **`cosmic_text::FontSystem::new()` performs a full, unconditional system font scan — every time a `TextPipeline` is constructed.** From `cosmic-text-0.18.2/src/font/system.rs:181-200`:
   ```rust
   pub fn new() -> Self {
       Self::new_with_fonts(core::iter::empty())
   }
   pub fn new_with_fonts(fonts: impl IntoIterator<Item = fontdb::Source>) -> Self {
       let mut db = fontdb::Database::new();
       Self::load_fonts(&mut db, fonts.into_iter());   // db.load_system_fonts() + any extra sources
       db.set_monospace_family("Noto Sans Mono");
       db.set_sans_serif_family("Open Sans");
       db.set_serif_family("DejaVu Serif");
       Self::new_with_locale_and_db_and_fallback(locale, db, PlatformFallback)
   }
   ```
   `load_fonts` (`system.rs:423-440`, `std` feature) calls `db.load_system_fonts()` unconditionally — there is no way to opt out short of not calling `FontSystem::new()` at all (akar would have to call `FontSystem::new_with_locale_and_db(locale, db)` with a caller-built `db` instead). cosmic-text's own doc comment on `FontSystem::new()` (`system.rs:174-180`) states: *"This function takes some time to run. On the release build, it can take up to a second, while debug builds can take up to ten times longer."* This is a real, documented cold-start cost, not speculation.

4. **`fontdb::Database::load_system_fonts()` scans hardcoded, platform-specific directories** (`fontdb-0.23.0/src/lib.rs:400-476`): macOS scans `/Library/Fonts`, `/System/Library/Fonts`, `/System/Library/AssetsV2/com_apple_MobileAsset_Font*`, `/Network/Library/Fonts`, `~/Library/Fonts`; Windows scans `%SYSTEMROOT%\Fonts` plus two per-user font dirs; Linux uses `fontconfig` if the `fontconfig` cargo feature is enabled (it is — see point 6) or else scans `/usr/share/fonts`, `/usr/local/share/fonts`, `~/.fonts`, `~/.local/share/fonts`. These directory sets differ across the three OSes akar targets, and even on one OS, contents vary by machine (user-installed fonts, OS version, MDM-pushed corporate fonts, etc.).

5. **The default generic-family names cosmic-text configures are not guaranteed to exist on any given machine, and when they don't, fallback silently becomes "best-scoring face across the entire scanned system font set," not "no font."** `fontdb::Database::query()` (`fontdb-0.23.0/src/lib.rs:661-679`) does an exact string match against `face.families` for the resolved generic family name (`"Open Sans"`, `"DejaVu Serif"`, `"Noto Sans Mono"` — none of which ship by default on stock macOS, and are not guaranteed on Windows or a minimal Linux install either) and returns `None` if no face has that exact family name. But `cosmic_text::FontSystem::get_font_matches` (`system.rs:349-407`) does **not** stop there: it builds `font_match_keys` by mapping **every face in the database** (`self.db.faces()`, no family filter at all) through `FontMatchKey::new(attrs, face)` (`system.rs:31-59`), which scores purely on `not_emoji`, `weight_diff`, `stretch_diff`, `style_diff`, `weight`, `stretch`, `id` — **family name is not part of this scoring at all**. It then sorts this family-agnostic list, and only tries to move the `db.query()` result (if any) to the front. In other words: if the configured generic family isn't installed, shaping falls back to "closest weight/style match among every font fontdb happened to scan," which is a function of exactly what's installed on that machine and in what `fontdb::ID` order it was discovered — not a deterministic, akar-controlled fallback chain.

6. **`cosmic-text`'s `fontconfig` feature is enabled by default and pulled in transitively.** `cosmic-text`'s packaged `Cargo.toml` (`cosmic-text-0.18.2/Cargo.toml`) declares `default = ["std", "swash", "fontconfig"]`, and `fontconfig = ["fontdb/fontconfig", "std"]`. akar's `Cargo.toml` pins `cosmic-text = "0.18"` only through `glyphon` (glyphon's own `Cargo.toml` depends on `cosmic-text = "0.18"` with default features) — akar does not disable this. On Linux this means font discovery additionally depends on the local `fontconfig` installation and its config files (`fontconfig_parser` reads `FONTCONFIG_FILE`/`fontconfig.conf`), adding a third axis of machine-dependence beyond "which directories exist."

7. **Bytes-based custom font loading is already a fully worked-out, straightforward API one layer down — akar just doesn't call it.** `fontdb::Database::load_font_data(&mut self, data: Vec<u8>)` (`fontdb-0.23.0/src/lib.rs:195-197`) and `load_font_source` (`lib.rs:203-229`) parse font bytes (including `.ttc`/`.otc` collections, loading every face) into the database with no filesystem or platform dependency — this is exactly the C-ABI-friendly primitive the epic's Research hypothesis called for. `cosmic_text::FontSystem::new_with_fonts(fonts: impl IntoIterator<Item = fontdb::Source>)` (`system.rs:186-200`) and `FontSystem::db_mut()` (`system.rs:281-284`, which clears the match cache on access — important for correctness after loading new fonts post-construction) are the two hook points akar would use. `cosmic_text::Family::Name(&str)` (re-exported as `glyphon::Family::Name`, confirmed via `~/Projects/glyphon/src/lib.rs:25-30`) is the mechanism for referencing an arbitrary loaded family by name in `Attrs` — akar's `FontFamily` enum (`crates/akar-components/src/text_style.rs:3-8`) only exposes the three CSS generic buckets (`SansSerif | Serif | Monospace`) and has no variant for it today.

8. **akar's existing typography boundary (epic 020) is `TextStyle`/`FontFamily`/`FontWeight` in `akar-components`, not a type literally named `AkarTypography`.** (The epic's introduction used "`AkarTypography` and friends" as informal shorthand; grepping the codebase for `AkarTypography` returns zero hits outside this epic file.) The real types: `crates/akar-components/src/text_style.rs:1-122` defines `#[repr(C)] pub enum FontFamily { SansSerif, Serif, Monospace }` (line 3-8), `#[repr(C)] pub enum FontWeight { Normal, Medium, Semibold, Bold }` (line 12-18), `pub struct TextStyle { font_size, line_height, color, font_weight, font_family, align, wrap }` (all `Option<_>`, line 30-38, for override cascading), a crate-private `ResolvedTextStyle` (line 56-64), and `resolved_to_attrs`/`resolved_to_metrics` (line 104-122) which are the sole place `ResolvedTextStyle` becomes a `glyphon::Attrs`/`glyphon::Metrics`. This is the one and only seam where a font-family choice turns into a glyphon call — any new font/fallback API must plug in here (or extend it) to affect what actually renders. `heading.rs`, `link.rs`, `paragraph.rs` all consume this same `resolve_text_style`/`resolved_to_attrs` pair with component-specific defaults (e.g. `heading.rs:34` hardcodes `font_family: FontFamily::SansSerif`).

9. **The C ABI mirrors this 1:1 today, with no headroom for custom families.** `crates/akar-c-api/src/lib.rs:27-52` defines `#[repr(C)] pub struct AkarFontFamily { pub value: u32 }` and `AkarFontWeight { pub value: u32 }` — **plain numeric-tag wrapper structs, not native C enums** (there is no C `enum AkarFontFamily { AKAR_FONT_FAMILY_SANS_SERIF, ... }` anywhere in the header; the `SansSerif | Serif | Monospace` variant names exist only on the Rust-side `FontFamily` enum in `akar-components`) — plus a C `AkarTextStyle`-equivalent struct with sentinel values (`SENTINEL_F32`/`SENTINEL_U32`, lines 12-14) standing in for `Option::None` across the ABI (there is no `Option<T>` in C, so this sentinel pattern is the established akar convention for "unset" — any new font-config C struct should reuse it). Lines 206-223 (`c_text_style_to_rust`) map the raw `u32` family/weight codes carried in `AkarTextStyle.font_family`/`font_weight` back to the Rust enums 0..=2 / 0..=3. There is no C entry point anywhere in this file for loading font bytes, setting a fallback chain, or registering a named family — `grep -n "load_font\|font_family.*byte\|fallback" crates/akar-c-api/src/lib.rs` returns nothing beyond the existing `FontFamily`/`FontWeight` style-resolution code.

10. **No prior art in the downstream reference app.** `~/Projects/daftprompt/src/` has zero references to fonts (`grep -rl "font\|Font" ~/Projects/daftprompt/src/` returns nothing) — daftprompt has not yet needed to touch font loading, so there is no existing consumer expectation to preserve compatibility with.

11. **Reproducibility impact is concrete, not hypothetical, given point 5.** akar's own design goals (`DEVELOP.md` "Screenshot Workflow": *"works identically on macOS, Windows, and Linux"*) and the CI regression gate (`epics/014-screenshot-enhancements.md`, `akar-diff --compare --threshold`) assume pixel-identical rendering across machines for the same input. Given that (a) `load_system_fonts()` scans OS- and machine-specific directories, (b) the configured generic-family names are not present on a stock macOS install (verified by reading fontdb's own macOS-default generic family names at `fontdb-0.23.0/src/lib.rs:181-188` — `Database::new()`'s own baked-in defaults are `"Arial"`/`"Times New Roman"`/`"Courier New"`, which cosmic-text then *overrides* to `"Open Sans"`/`"DejaVu Serif"`/`"Noto Sans Mono"` at `system.rs:195-197`, none of which are stock macOS fonts), and (c) the no-exact-match fallback path is family-blind best-weight-match over whatever got scanned (point 5), the specific glyph outlines akar renders today for default (unstyled) text are **not architecturally guaranteed to be the same font across two different machines**, even both running macOS, let alone across macOS/Windows/Linux. This was not empirically re-verified by deleting/hiding a font on this machine (out of scope per task instructions — no build/GPU verification required), but it follows directly and unambiguously from the constructor and matching code read above, not from speculation about behavior.

---

## Review — 2026-08-12 verification pass

A verification-only pass (no source files changed; only this epic file was edited) re-checked every "Confirmed finding" and every `file:line` citation above against the current tree, and resolved the open questions the previous pass left behind. This machine has a real GPU and display, so the previous pass's "no build/GPU verification possible" premise does not apply here.

### Verified by execution on this machine

- `cargo test --workspace` — passes: 354 tests across the workspace (204 + 42 + 32 + 30 + 16 + 11 + 10 + 5 + 3 + 1), 0 failures.
- `cargo run --bin demo-rust -- --screenshot <path> --delay 0.5 --exit` — runs and writes a PNG. The debug toolchain is fully usable here; anything in this epic marked "not verified because no GPU/display" is verifiable and, where it mattered, has now been verified.
- A throwaway probe crate outside the repo (registry `glyphon = "0.11"`, same version as the workspace pin) was used to exercise cosmic-text directly. Results, on this stock macOS 24.3.0 machine:
  - `FontSystem::new()` scans **800 faces**; reported locale `en-US`.
  - `db.query()` for the generic sans-serif family returns **`None`** — cosmic-text's configured `"Open Sans"` is genuinely not installed. This empirically confirms Research points 5 and 11, which were previously source-reasoning only.
  - Shaping `"Hello world"` at 16px through the generic sans-serif attrs actually resolves to **`.SFNS-Regular`** (San Francisco) purely via the family-blind fallback scoring, width `74.68`. The same string against a database seeded only with IBM Plex Sans Regular measures `82.13` — a ~10% layout-affecting difference, i.e. the cross-machine reproducibility risk is real and measurable, not theoretical.
  - **New, important: an empty `fontdb::Database` panics.** `FontSystem::new_with_locale_and_db("en-US", Database::new())` followed by any `shape_until_scroll` panics with `no default font found` at `cosmic-text-0.18.2/src/shape.rs:315` (`font_iter.next().expect("no default font found")`). It does **not** degrade to zero-size or tofu. This invalidates the assumption written into Task 5 below; see the correction there.
  - A database seeded with exactly one Latin font renders **tofu, not a panic**, for uncovered scripts: `"你好世界"` against an IBM-Plex-Sans-only database produced 4 glyphs, width `30.21`. So "at least one font present" is the real precondition, and missing script coverage degrades gracefully.
  - With system scanning on, cosmic-text's automatic per-codepoint fallback works today with zero akar code: `"مرحبا بالعالم"` shaped to 13 glyphs, all with `level.is_rtl() == true`, using `.SFNS-Regular` + `GeezaPro`; `"你好世界"` used `ArialUnicodeMS`. This supports the Proposed API's recommendation (a) — rely on cosmic-text's automatic fallback for v1 rather than implementing akar-level family chains.

### Corrections to the findings above

- Research point 3 / Task 1: `FontSystem::new()` is at `cosmic-text-0.18.2/src/font/system.rs:181-183`; `new_with_fonts` at `186-200`. The snippet quoted above **omits the first line of `new_with_fonts`**, `let locale = Self::get_locale();` (`system.rs:187`). `load_fonts` (std) is at `system.rs:423-441` with `db.load_system_fonts()` at `system.rs:428` — as cited. The doc-comment timing quote is at `system.rs:174-180` — as cited.
- Research point 5: `FontMatchKey` is at `system.rs:18-27` and its fields are `pub(crate)`, not public, and are named `not_emoji`, `font_weight_diff`, `font_stretch_diff`, `font_style_diff`, `font_weight`, `font_stretch`, `id` (the epic wrote `weight_diff`/`stretch_diff`/`style_diff`). `FontMatchKey::new` is at `system.rs:30-58` (epic said 31-59). `get_font_matches` is at `system.rs:349-410` (epic said 349-407). The substance of the finding — family name is not part of the score, and the whole `db.faces()` set is ranked — is correct as written and is now also confirmed empirically.
- Research point 9 / Task 1: `crates/akar-c-api/src/lib.rs:12-14` are `SENTINEL_F32`, `SENTINEL_U8` (the epic omitted this one) and `SENTINEL_U32`. `AkarFontFamily` is at `lib.rs:29-32`, `AkarFontWeight` at `34-37`, `AkarTextAlign` at `39-42`, `AkarTextStyle` at `44-53`. `c_text_style_to_rust` starts at `lib.rs:187`, not 206; the weight/family/align decode block is `lib.rs:206-232`, with the family arm at `216-224`. Everything else in point 9 holds.
- Research point 11: fontdb's own `Database::new()` generic-family defaults are at `fontdb-0.23.0/src/lib.rs:178-190` (doc comment `170-176`); the epic cited `181-188`, close but off. Values (`Arial`/`Times New Roman`/`Courier New`, `Papyrus` for fantasy on macOS) are as stated.
- Research point 1: the wholesale re-export is `~/Projects/glyphon/src/lib.rs:25-31` (the `// Re-export ...` comment is at 23). The local glyphon checkout is version `0.11.0`, matching `Cargo.lock:850-853` — the checkout is a valid source of truth for what akar compiles.
- Research point 6 confirmed: `cosmic-text-0.18.2/Cargo.toml:33-40` declares `default = ["std", "swash", "fontconfig"]` and `fontconfig = ["fontdb/fontconfig", "std"]`. Additionally verified for implementation purposes: cosmic-text takes fontdb with `default-features = false` (`Cargo.toml:115-117`) but its `std` feature enables `fontdb/memmap`, which enables `fontdb/fs` (`fontdb-0.23.0/Cargo.toml:62-78`) — so `Database::load_system_fonts()`, which is `#[cfg(feature = "fs")]` (`fontdb-0.23.0/src/lib.rs:399-400`), **is** available to akar through `glyphon::fontdb`. No feature-flag work is needed to call it.
- akar-side citations all still resolve: `text_pipeline.rs:36-59` (`TextPipeline::new`, `FontSystem::new()` at `41`), `text_pipeline.rs:385-398` (`shaped_buffer`, its own `FontSystem::new()` at `386`, used at `432`/`446`/`459`), `text_pipeline.rs:400-412` (`create_pipeline`), `context.rs:24-42` (`AkarCore::new`, `TextPipeline::new` at `35`), `context.rs:48-60` (`mock`), `akar-c-api/src/lib.rs:368-395` (`akar_ctx_new`) and `407-419` (`akar_ctx_mock`), `akar-components/src/text_style.rs` `3-8`/`12-18`/`30-38`/`54-64`/`103-117`/`119-122`, and `heading.rs:34`, `link.rs:24`, `paragraph.rs:18`. `Cargo.lock:514-517` (cosmic-text 0.18.2) and `725-728` (fontdb 0.23.0) are correct.

### Open questions resolved in this pass

1. **Locale detection (the `FontSystem::new_with_locale_and_db` snippet).** `sys_locale` is **not** a direct akar dependency — it appears in `Cargo.lock:2149-2152` (`sys-locale 0.3.2`) only transitively, pulled in by cosmic-text's `std` feature (`cosmic-text-0.18.2/Cargo.toml:52-59`). It is therefore not usable from `akar-core` without adding it explicitly. cosmic-text's own detection is `FontSystem::get_locale()` at `system.rs:411-417`, a **private** associated function: `sys_locale::get_locale().unwrap_or_else(|| "en-US")`. What the locale is actually used for: it is threaded into `Fallback::script_fallback(script, locale)` (`cosmic-text-0.18.2/src/font/fallback/mod.rs:76`, used at `mod.rs:114` and `144`) — i.e. it only influences per-script fallback preference, most visibly Han unification (zh vs. ja vs. ko glyph choice). **Decision for v1: hardcode `"en-US"`.** Rationale: it is the same value cosmic-text falls back to when detection fails, it adds no dependency, and — decisively for this epic — a machine-dependent locale is exactly the kind of non-determinism Task 2 is trying to remove. Add a `locale: Option<String>` field to `TextPipelineConfig` later if an application needs CJK-language-correct fallback; adding `sys-locale = "0.3"` as a direct dependency is then the mechanism, not `cosmic_text`'s private helper.
2. **Does `AkarCore::mock()` depend on real glyph measurement? Yes.** `crates/akar-layout/tests/text_measurement.rs` builds its cores with `AkarCore::mock()` (`text_measurement.rs:12-14`) and asserts real, non-zero shaped geometry — e.g. `single_line_intrinsic_size` asserts `size.width > 0.0` and `size.height > 0.0` (`text_measurement.rs:48-68`), and `wrapped_multi_line_height` compares wide vs. narrow measured heights (`text_measurement.rs:70+`). `crates/akar-core/src/text_pipeline.rs`'s own measurement tests (`measure_with_metadata_*`, lines `467-541`) go through `create_pipeline()`, which likewise needs a populated font database. Combined with the empty-database **panic** finding above, this means `mock()` cannot ship with an empty database even temporarily: Tasks 5 and 6 must land in the same change, or Task 5 must keep `mock()` on `BundledPlusSystemScan` until Task 6 lands. There are 122 `AkarCore::mock()` call sites across `crates/`, most of which draw text.
3. **Which font to bundle, and is its license MIT-compatible?** Recommendation: **IBM Plex Sans Regular**, under the **SIL Open Font License 1.1**. This is verifiable entirely locally, without any network access: the font file is present at `~/Projects/zed/assets/fonts/ibm-plex-sans/IBMPlexSans-Regular.ttf` (200,500 bytes) and its full license text at `~/Projects/zed/assets/fonts/ibm-plex-sans/license.txt` (4,360 bytes), which was read in this pass. The OFL 1.1 permission grant covers "use, study, copy, merge, embed, modify, redistribute, and sell modified and unmodified copies" (`license.txt:48-51`). The conditions that bind akar (`license.txt:53-77`): (1) the font may not be sold by itself; (2) it may be bundled/redistributed with any software provided each copy carries the copyright notice and the OFL text — a stand-alone text file next to the font satisfies this; (3) a modified version may not reuse the reserved font name "Plex"; (5) the font itself must stay under the OFL and must not be relicensed. **None of these conflict with akar's MIT `LICENSE`**: MIT governs akar's own code, the OFL continues to govern the bundled font binary, and both permit redistribution inside a compiled binary. The concrete obligation on the implementer is to copy the OFL text into the repo alongside the `.ttf` and note the dual-licensing situation in `LICENSE` or a `NOTICE`. A verified alternative if a monospace or a different look is wanted: Lilex (`~/Projects/zed/assets/fonts/lilex/`, OFL 1.1, `OFL.txt` read and identical in substance). egui also ships `Ubuntu-Light.ttf` under the **Ubuntu Font Licence** (`~/Projects/egui/crates/epaint_default_fonts/fonts/UFL.txt`) and `Hack-Regular.ttf` under its own licence — both are more restrictive/unusual than OFL and are **not** recommended. Note the honest limit of this verification: the license texts read are the ones distributed alongside those font files in those local checkouts; they were not cross-checked against upstream, and no font file has been added to this repo (`find` for `*.ttf|*.otf|*.ttc|*.woff*` outside `target/` returns nothing today).
4. **Do the proposed C ABI names match what exists?** Partly. `akar_load_font_bytes` and `AkarFontSource` are new names that collide with nothing (`grep` for `load_font`/`fallback`/`font_source` in `crates/akar-c-api/src/lib.rs` returns no hits). But **`akar_text_style_set_family_name(AkarTextStyle*, const char*)` does not fit the existing type**: `AkarTextStyle` (`lib.rs:44-53`) is a flat POD of scalars that the *caller* constructs and passes as `*const AkarTextStyle`; akar never owns or allocates one, so there is nothing for a setter to write into that akar could later free. See the revised Task 7 for the replacement design.
5. **Are there external callers of `akar_ctx_new` to update (Task 8)?** No. The only occurrences are the definition (`crates/akar-c-api/src/lib.rs:368`) and the generated declaration (`akar.h:288`). The C integration test uses `akar_ctx_mock()` (`crates/akar-c-api/tests/text_edit.c:39`) and the Rust tests in `lib.rs` use `akar_ctx_mock()` (122 sites use `AkarCore::mock()`, and the c-api tests use `akar_ctx_mock`). Changing `akar_ctx_new`'s signature therefore breaks no in-repo caller.
6. **`akar.h` regeneration flow (Task 8).** It is automatic: `crates/akar-c-api/build.rs` runs `cbindgen::generate` on every build and writes the workspace-root `akar.h` (config at `crates/akar-c-api/cbindgen.toml`). No manual command is needed — just build, then commit the regenerated header.
7. **Asset-storage convention (Task 6).** There is none for fonts: no font file of any kind exists in the repo. Top-level `images/`, `misc/`, and `website/` exist but are unrelated. `crates/akar-core/assets/fonts/` is the recommended location, since `include_bytes!` must resolve relative to the crate that embeds it.
8. **Demo script location (Task 9).** Confirmed to exist: `examples/demo-rust/scripts/` contains `open_dropdown.txt`, `text_edit_backspace.txt`, `text_edit_custom_bindings.txt`, `text_edit_paste.txt`, `text_edit_shortcuts.txt`.

### Additional correction to the Proposed API (Rust `FontFamily::Named`)

Two concrete compile-level problems with the `Named(String)` proposal, found by reading the actual types rather than assuming:

- `resolved_to_attrs<'a>(rt: &ResolvedTextStyle) -> glyphon::Attrs<'a>` (`text_style.rs:103-117`) has an **unconstrained** output lifetime today, which only works because every current arm produces a borrow-free `glyphon::Family` variant. `glyphon::Family::Name(&name)` borrows from `rt`, so the signature must become `fn resolved_to_attrs<'a>(rt: &'a ResolvedTextStyle) -> glyphon::Attrs<'a>`. All three call sites (`heading.rs:58`, `link.rs:55`, `paragraph.rs:41`) bind the result to a local that outlives nothing else, so this change is mechanical there.
- The `Copy` loss is wider than the epic states. `TextStyle` (`text_style.rs:29`) and `ResolvedTextStyle` (`text_style.rs:54`) both derive `Copy`, and `resolve_text_style` relies on it twice: `let mut resolved = *defaults;` (`text_style.rs:74`) and the `if let Some(v) = style.font_family` arm (`text_style.rs:89-91`), which moves a `Copy` value out of a shared reference. With a `String` inside, both need `.clone()` and both structs lose `Copy`, which ripples to every component that copies a `TextStyle` by value.

Given that, the recommended v1 design is **Option B: a numeric family handle** — `FontFamily::Named(u32)`, where the `u32` is returned by `TextPipeline::load_font_bytes` / `akar_load_font_bytes` and indexes an akar-owned table of loaded family names. This keeps `FontFamily` `Copy`, `Eq` and `#[repr(C)]`, keeps `TextStyle`/`ResolvedTextStyle` `Copy`, keeps the C `AkarTextStyle` a flat POD (the existing `font_family: u32` field can carry `3 + handle`, or a separate `font_family_name_handle: u32` field with `SENTINEL_U32` for unset), and needs no string lifetimes across the FFI boundary at all. `resolved_to_attrs` then needs access to the handle table, so it takes an extra `&FontRegistry` argument (or the lookup happens in the caller, which already has the pipeline). **Option A (`Named(String)`)** remains viable if the maintainer prefers a string-keyed API; it costs the `Copy` removals and lifetime change described above, plus a `const char *font_family_name` field on `AkarTextStyle` (NULL = unset) decoded in `c_text_style_to_rust` (`lib.rs:216-224`) — not a setter function, since akar does not own that struct.

### Readiness audit — 2026-08-12

The source and git-history audit found that the numeric-handle direction is sound for the C ABI, but the proposed implementation is not yet compile-ready:

1. **`load_font_data` cannot implement the proposed `Result`.** In pinned `fontdb 0.23.0`, `Database::load_font_data(Vec<u8>)` returns `()` and silently logs/skips faces that fail parsing. `Database::load_font_source(Source::Binary(Arc<Vec<u8>>))` returns all inserted face IDs (`TinyVec<[ID; 8]>`). Task 5 must use `load_font_source`, treat an empty returned ID list as `InvalidFontData`/`EmptyFontSource`, and inspect every returned `FaceInfo`. This also exposes a semantic issue hidden by the singular return type: TTF/OTF normally yields one face, but TTC/OTC may yield several faces and more than one family. `akar_load_font_bytes -> u32 family_handle` is therefore ambiguous. Resolve this before coding by either (recommended) registering every distinct loaded family and returning a small load-result/list API, or explicitly restricting v1 to sources containing exactly one distinct family and returning an error otherwise.
2. **A handle-table lookup cannot be added to `resolved_to_attrs` as proposed.** The registry naturally belongs to `akar-core::TextPipeline`, while `resolved_to_attrs` lives in `akar-components`. More importantly, resolving a handle to `&str` borrowed from `core.text_pipeline` and then calling `core.text_pipeline.set_text(..., Attrs<'_>)` requires a mutable borrow of the same pipeline while the immutable name borrow is live. The current component call path (`heading.rs`, `paragraph.rs`, `link.rs`) constructs `Attrs` before the mutable `set_text` call, so this design is rejected by Rust's borrowing rules even if the crate dependency direction is allowed. Keep `FontFamily::Named(u32)`, but pass an **owned font selection request** (generic family or numeric handle, plus weight) into a new `TextPipeline` entry point; resolve the handle and construct the short-lived `glyphon::Attrs` inside `TextPipeline`, immediately before `Buffer::set_text`. Generic-family call sites can migrate to the same path. Do not expose or borrow registry strings from components.
3. **Handle/sentinel rules need to be explicit.** `SENTINEL_U32` is currently `0xFF`, so it is unsafe as an unbounded handle sentinel: handle 255 would collide. The new named-family field should use a dedicated `u32::MAX` sentinel (or guarantee checked allocation below 255, which is unnecessarily restrictive). Keep the existing `0xFF` sentinel for the old small enums to avoid unrelated ABI churn.
4. **The advertised ordered fallback-chain goal is not delivered in v1.** The proposal correctly defers akar-owned chains and relies on cosmic-text's automatic script fallback. Remove `fallback_families` from the v1 `TextPipelineConfig` and the unimplemented `akar_set_fallback_families` declaration; describe explicit ordered chains as deferred, rather than retaining dead configuration fields that imply behavior they cannot provide.

With those amendments, the implementation sequence is: finalize the load-result/cardinality rule and the owned `TextSelection`/`TextAttrs` core type; implement Tasks 5+6 together; implement Task 7; then Task 8 and Task 9. No additional dependency-source research is needed.

### Decisions — 2026-08-14

Both open items from the readiness audit are now decided. No further design work is needed before implementing Tasks 5 and 7.

1. **Multi-family load result (audit item 1): v1 restricts to single-family sources.** `TextPipeline::load_font_bytes` / `akar_load_font_bytes` calls `load_font_source`, inspects every returned `FaceInfo`, and collects the set of distinct family names across them. If that set has exactly one member, register it and return its handle as normal. If the set is empty, return `FontLoadError::EmptyFontSource`. If the set has more than one member (only possible with a `.ttc`/`.otc` collection spanning multiple families — a single `.ttf`/`.otf`, the format an application actually bundles, always yields exactly one), return a new `FontLoadError::MultipleFamilies` and load nothing. Rationale: collections are rare for bundled/app-supplied fonts (they're mostly a system-font packaging artifact — see Research point 4's `PingFang.ttc`/`Songti.ttc`-style examples) and this keeps the C ABI's single-`u32`-handle-per-call contract unambiguous. A list-returning API for multi-family collections can be added later purely additively (a new `akar_load_font_source_multi` alongside the existing function) without breaking this one. `FontLoadError` (Proposed API → "Rust: `akar-core`") gains this variant:
   ```rust
   #[derive(Debug, Error)]
   pub enum FontLoadError {
       #[error("font data could not be parsed: {0}")]
       InvalidFontData(String),
       #[error("no faces found in font data")]
       EmptyFontSource,
       #[error("font data contains {0} distinct families; v1 requires exactly one")]
       MultipleFamilies(usize),
   }
   ```
2. **Where the handle resolves (audit item 2): adopt the owned-selection-request design as specified, unchanged.** Confirmed as forced by the borrow checker, not a style preference: `resolved_to_attrs` lives in `akar-components` and cannot borrow a `&str` out of `akar-core`'s font registry while `TextPipeline::set_text` needs `&mut` access to that same pipeline. `FontFamily::Named(u32)` stays as designed (Research/Review section, "Additional correction to the Proposed API"). What changes is where the handle is resolved: components build an owned selection request (a generic family variant, or a numeric handle, plus weight) and pass it into a new `TextPipeline` entry point (name TBD at implementation time, e.g. `TextPipeline::set_text_with_style` or similar), which resolves the handle against its own registry and constructs the short-lived `glyphon::Attrs` internally, immediately before `Buffer::set_text`. No registry string or `Attrs` borrowing a registry name ever crosses back out to `akar-components`. Generic-family (`SansSerif`/`Serif`/`Monospace`) call sites migrate to the same entry point for consistency, rather than keeping two separate text-setting paths.

   **Note on why this is the idiomatic fix, not a workaround.** The conflict is two incompatible borrows of one object: `Attrs<'a>` holding an immutable borrow of the registry for lifetime `'a`, alive at the same moment `set_text` needs `&mut` on that same pipeline. No lifetime annotation resolves that — it has to be restructured. The general Rust pattern for "look something up in a store, then mutate that same store using what was looked up" is to stop the borrowed data from crossing the API boundary at all: pass an owned, cheap descriptor in (here, the existing `Copy` `u32` handle), and do the lookup-then-mutate inside one function that takes `&mut self` once, so the borrow never outlives the call. Alternatives considered and rejected: cloning the family name into an owned `String` before calling `set_text` (works, but pays a per-call allocation for a lookup akar already does cheaply as `u32 → &str`); wrapping the registry in `RefCell` for interior mutability (trades a compile-time-provable non-issue for a runtime borrow check and a new panic path, and cuts against akar's synchronous, no-hidden-state design per `DEVELOP.md`); splitting `TextPipeline` into separate registry/renderer structs so each can be borrowed independently (a legitimate general technique, but more structural change than this problem needs). Resolve-then-mutate in one call, on an owned handle, is the smallest change that satisfies the borrow checker without sacrificing `Copy`/`repr(C)` on the public types.

3. **Bundle scope (new, 2026-08-14): bundle two faces — `IBMPlexSans-Regular.ttf` and `IBMPlexSans-SemiBold.ttf` — not Regular alone.** The original Task 6 decision said "IBM Plex Sans Regular", which is not enough. akar's `FontWeight` (`crates/akar-components/src/text_style.rs:12-18`) has four variants and components actively use the heavy end — `heading.rs:34` defaults to `FontWeight::Bold`. With a Regular-only database, fontdb's weight scoring has exactly one face to choose from and returns it regardless of the requested weight, so every bold heading would silently render at regular weight. That is a visible regression against today's behavior, where the system scan supplies a real bold face. Mapping with both faces bundled: `Normal`/`Medium` → Regular, `Semibold`/`Bold` → SemiBold. Approximate cost: 200,500 + 202,632 bytes ≈ 403 KB.

   Deliberately **not** bundled: `IBMPlexSans-Italic.ttf` and `IBMPlexSans-SemiBoldItalic.ttf`, both present in the same local checkout. akar has no italic/oblique axis anywhere in its public API — `TextStyle` (`text_style.rs:30-38`) carries no style field and `resolved_to_attrs` never sets `glyphon::Style` — so nothing could select them; they would be dead weight until a font-style axis is added. Known limitation to record in the implementation: the local checkout has no true `Bold` (700) face, so `FontWeight::Bold` resolves to SemiBold (600) until additional weights are sourced from upstream IBM Plex. This is a deliberate v1 accepted tradeoff, not an oversight.

4. **Font assets are committed to the repository, with a default-on `bundled-font` Cargo feature as the opt-out (new, 2026-08-14).** The question raised was whether the font files could be gitignored on the reasoning that downstream applications will select their own fonts. Downstream selection is real and fully supported — `TextPipelineConfig::fonts` and `TextPipeline::load_font_bytes` let any application supply its own fonts, and the bundled face is only the *default*, never a mandate — but that is satisfied by the API, not by omitting the files. Gitignoring them does not work mechanically: Task 6 embeds the font with `include_bytes!`, which resolves at compile time, so an ignored font file means `cargo build` fails outright on a fresh clone. Nor can the default simply be dropped: an empty `fontdb::Database` panics with `no default font found` at `cosmic-text-0.18.2/src/shape.rs:315` on the first shaping call (verified 2026-08-12), which would take out `AkarCore::mock()` and the 122 test call sites that shape text.

   Adopted design: commit both faces under `crates/akar-core/assets/fonts/`, and gate embedding behind a `bundled-font` feature in `crates/akar-core/Cargo.toml` that is **on by default**. Default builds stay reproducible out of the box with no caller action. An embedder who wants the ~403 KB back builds `akar-core` with `default-features = false`, in which case `TextPipelineConfig::default()` carries no fonts and the application **must** supply at least one via `fonts`/`load_font_bytes` before any text is shaped — document this as an explicit, named panic condition rather than letting cosmic-text's opaque `no default font found` surface to them. Feature-off builds are therefore not covered by the reproducibility guarantee. Keep `AkarCore::mock()` and the workspace test suite on the default feature set so no test path can reach an empty database.

Tasks 5 and 7 below are updated to reflect these as adopted decisions rather than open blockers; Task 6 is updated for decisions 3 and 4.

---

## Tasks

### Task 1 — Inventory Current Font Handling

**Status:** Done — re-verified 2026-08-12 against current source; all `file:line` citations in this task resolve correctly.
**Readiness:** Ready for implementation (nothing to implement — research task, complete)

- Read `TextPipeline` and `FontSystem` construction in `akar-core` end to end. `crates/akar-core/src/text_pipeline.rs:36-59` (`TextPipeline::new`) and the test helper at `text_pipeline.rs:381-412`; construction call chain traced to `AkarCore::new` (`crates/akar-core/src/context.rs:24-42`) and `akar_ctx_new`/`akar_ctx_mock` (`crates/akar-c-api/src/lib.rs:368-419`).
- **`fontdb` scans system fonts today; akar bundles nothing.** `TextPipeline::new` calls the zero-argument `glyphon::FontSystem::new()` (`text_pipeline.rs:41`), which resolves to `cosmic_text::FontSystem::new_with_fonts(core::iter::empty())` (`cosmic-text-0.18.2/src/font/system.rs:181-183`), which always calls `fontdb::Database::load_system_fonts()` (`system.rs:428`) and passes zero extra font sources. No bytes are bundled into the akar binary anywhere in `akar-core`/`akar-components`/`akar-c-api` (confirmed by grep for font-related byte literals/`include_bytes!` — none found).
- **No family/fallback configuration is exposed today, in Rust or C.** Grepped `crates/akar-c-api/src/lib.rs` for `load_font`/`fallback`/family-registration entry points — none exist beyond the existing generic `FontFamily`/`FontWeight` style enums (`crates/akar-components/src/text_style.rs:3-18`, mirrored in C at `crates/akar-c-api/src/lib.rs:27-52`), which only select among the three CSS generic buckets, not concrete font files or family names.
- **What glyphon expects from a host application vs. does internally**, read from `~/Projects/glyphon/src/lib.rs`, `text_render.rs`, `text_atlas.rs`: glyphon owns none of this. It re-exports `cosmic_text` (including `fontdb`) wholesale (`lib.rs:23-31`) and every glyphon entry point that touches text (`TextRenderer::prepare`, `TextAtlas` glyph rasterization) takes an already-constructed `&mut FontSystem` as a caller-supplied argument. glyphon never constructs, configures, or scans a `FontSystem`/`fontdb::Database` itself — that responsibility sits entirely with the host (here, `akar-core`), one layer below glyphon in `cosmic-text`/`fontdb`.
- Findings documented above in "Confirmed findings" (points 1-9).

### Task 2 — Reproducibility Constraint Check

**Status:** Done — and, as of 2026-08-12, no longer source-analysis-only. The core claim was confirmed empirically on this machine without modifying any system files: `db.query()` for the generic sans-serif family returns `None` (cosmic-text's `"Open Sans"` is not installed among the 800 scanned faces), and default sans-serif text actually shapes against `.SFNS-Regular` purely through the family-blind fallback scoring. Measured width for `"Hello world"` at 16px: `74.68` via the system scan vs. `82.13` against a bundled-only IBM Plex Sans database — a layout-visible difference. See "Review — 2026-08-12 verification pass".
**Readiness:** Ready for implementation (nothing to implement — research task, complete; the recommendation below stands and is now empirically supported)

- **Did not run `demo-rust --screenshot` with a system font hidden/renamed** (still true, and still the right call — but the 2026-08-12 pass reached the same conclusion non-invasively by querying the live font database directly; see the review section) — that would require mutating files outside the akar repo (e.g. `/System/Library/Fonts`) on this machine, which is an invasive, side-effecting system change disproportionate to a research task and was avoided. `cargo check --workspace` was run (succeeds) purely to confirm the pinned dependency versions read match what's actually compiled.
- Instead, the non-determinism was established by reading `fontdb`'s scan and query code directly (Research points 4-5, 11): `load_system_fonts()` (`fontdb-0.23.0/src/lib.rs:400-476`) scans OS-specific, hardcoded directory lists whose contents are inherently machine-dependent (user-installed fonts, OS version, corporate MDM font pushes), and `Database::query()` (`lib.rs:661-679`) exact-matches against generic family names (`"Open Sans"`, `"DejaVu Serif"`, `"Noto Sans Mono"`, set at `cosmic-text-0.18.2/src/font/system.rs:195-197`) that are **not** pre-installed on stock macOS (confirmed: macOS ships Helvetica/San Francisco/Times/Courier/Arial, not Open Sans/DejaVu Serif/Noto Sans Mono) and are not guaranteed on Windows or a minimal Linux install. When the exact match fails, `FontSystem::get_font_matches` (`system.rs:349-407`) falls back to scoring *every* scanned face by weight/stretch/style only (`FontMatchKey::new`, `system.rs:31-59` — no family term in the score at all), so the actual font selected becomes a function of whatever the local machine happened to have installed and in what `fontdb::ID` discovery order. This is a structural, code-level guarantee of possible divergence, not merely a suspicion — no live test was needed to establish it, though a live test would still be valuable as a regression check once the fix lands (see Task 8 below).
- **Impact on `akar-diff --compare` CI regression gating (`epics/014`):** `akar-diff` is documented as pixel-exact for v1 (`DEVELOP.md` "Screenshot Workflow" → "Remaining limitations"). Pixel-exact diffing against a checked-in baseline PNG is only meaningful if the renderer is deterministic given the same input and akar version. Per the above, default (unstyled) text glyph outlines are not guaranteed identical across machines/OSes today, which means any screenshot baseline containing default-styled text is a latent CI flake risk the moment `akar-diff --compare` runs on a second machine — this has apparently not yet bitten the project (epics/014 itself doesn't call out fonts), likely because CI visual regression is explicitly punted as of epics/014 Task 014e (`DEVELOP.md`: *"No headless/offscreen rendering... a future epic will address it when CI visual regression is prioritized"*) — i.e. the gate isn't running on multiple machines yet, so the risk hasn't surfaced, but it is a live landmine for whenever that punt is lifted.
- **Recommendation: bundle a default font, keep system scanning strictly opt-in.** Concretely: `TextPipeline` should construct `FontSystem` via `new_with_locale_and_db` (or equivalent) over a `fontdb::Database` seeded only with one or more bundled font(s) (`Database::load_font_data`, no `load_system_fonts()` call) unless the application explicitly opts into system scanning through the new API proposed in Task 4. This makes the *default* out-of-the-box akar behavior byte-for-byte reproducible across machines and OSes (satisfying `DEVELOP.md`'s "identical screenshot" goal and de-risking the eventual CI visual-regression gate), while still letting applications opt into system fonts for broader glyph coverage (CJK/Arabic/etc.) when reproducibility is not their priority for that surface. This does not conflict with bytes-based custom font loading (Research point 7) — both can coexist as explicit, opt-in `FontSource` variants.

### Task 3 — Prototype Non-Latin Rendering

**Status:** Superseded by Task 9 — the projections below were checked on 2026-08-12 with an out-of-repo probe crate (no akar source touched) and hold up. Measured on this machine with a system-scanning `FontSystem`: `"مرحبا بالعالم"` shapes to 13 glyphs, all with `level.is_rtl() == true`, using `.SFNS-Regular` + `GeezaPro`; `"你好世界"` shapes to 4 glyphs using `ArialUnicodeMS`. So cosmic-text's automatic per-codepoint fallback already does the work, and `Family::Name` selection of an explicitly loaded font also works (verified: a database seeded only with `IBMPlexSans-Regular.ttf` exposes family `"IBM Plex Sans"` and shapes correctly through both `Family::Name("IBM Plex Sans")` and the generic sans-serif attrs). Uncovered scripts degrade to tofu, not a panic (4 glyphs, width `30.21` for `"你好世界"` against the Latin-only database). The remaining unverified part — that this renders correctly *through akar's public API and pixel pipeline* — is exactly Task 9.
**Readiness:** Ready for implementation as Task 9 (do not re-attempt this task in its original bypass-spike form)

This task as written ("load a CJK font and an Arabic font by bytes... render... capture screenshots... note glyph/fallback/shaping failures") requires modifying `TextPipeline` or writing bypass code and driving `demo-rust`, which is implementation work, not investigation — out of scope for this pass per the constraints given to this research task. What follows is a source-analysis-based projection of what would happen, clearly labeled as unverified-by-execution:

- **Loading would work mechanically with no code changes to `cosmic-text`/`fontdb`.** `fontdb::Database::load_font_data(Vec<u8>)` (`fontdb-0.23.0/src/lib.rs:195-197`) parses arbitrary font bytes (TTF/OTF/TTC/OTC) with no OS or filesystem dependency; a Noto Sans CJK or Noto Naskh Arabic TTF loaded this way would populate `FontSystem`'s `db` with real, queryable `FaceInfo` entries. This part is a solved problem one layer below akar and is expected to work.
- **CJK rendering: Latin shaping path is `Shaping::Advanced` already (`text_pipeline.rs:91`), and `harfrust`/`rustybuzz`-class shapers used by cosmic-text 0.18 handle CJK shaping (mostly 1:1 codepoint-to-glyph, no complex reordering) without special akar-side logic — the risk is *font coverage*, not shaping.** If the loaded CJK font's family name is referenced via `glyphon::Family::Name("...")` in `Attrs` (confirmed to exist and be re-exported, Research point 7) it should render correctly. Since akar's public `FontFamily` enum has no `Name(&str)`-equivalent variant today (Research point 9), this can only be reached by bypassing the public component API and constructing `glyphon::Attrs` directly — exactly the "bypass the public API" caveat the task called out.
- **Arabic rendering is the higher-risk case, structurally, not just a coverage question.** Arabic requires (a) correct RTL bidi handling and (b) correct contextual shaping (initial/medial/final/isolated glyph forms) — `cosmic-text` includes `unicode-bidi` as a dependency (seen in the extracted `Cargo.toml`) and `harfrust`, a HarfBuzz-derived shaper, both of which are exactly the components needed. `text_pipeline.rs`'s own caret/selection geometry code (`text_geometry`, `glyph_boundary_x`, `caret_x`, lines 242-379) already branches on `glyph.level.is_rtl()`, indicating akar's text-editing geometry code has *some* existing RTL awareness baked in from the `LayoutGlyph`/`LayoutRun` types cosmic-text produces — this is encouraging evidence that bidi-level info flows through today, but whether visual shaping/glyph selection is fully correct for a *loaded* Arabic font specifically was not verified by execution.
- Epic 023 (RTL text rendering) is the more appropriate place to actually execute and screenshot-verify Arabic/RTL rendering; this epic's role, per its own framing ("soft prerequisite for 023/024"), is to make sure the *font* is loadable and selectable in the first place. Recommend Task 3 be re-scoped as an early task inside the *implementation* phase of this epic (see Task 6 below) rather than attempted again as pure research — it needs the loading API from Task 4/5 to exist before there's a supported way to do it that isn't a throwaway spike.

### Task 4 — API and Scope Proposal

**Status:** Done. Locale and broad API direction were resolved in the original research pass; the load-result cardinality and owned core-side font-selection request flagged in "Readiness audit — 2026-08-12" are now resolved in "Decisions — 2026-08-14".
**Readiness:** Ready for implementation — no open design amendment, no further dependency research needed.

See "Proposed API" and "Explicit Deferrals" sections below, read together with the 2026-08-12 review section and the 2026-08-14 decisions section, which override them where they conflict.

---

## Proposed API

Design goals, in priority order: (1) default construction stays fully reproducible (Task 2 recommendation — no implicit system scan), (2) applications can load font bytes and reference loaded families, (3) opt-in system scanning remains available for apps that want broad glyph coverage over reproducibility, (4) every new type is C-ABI-flat and follows akar's existing sentinel/enum conventions (`crates/akar-c-api/src/lib.rs:12-14`, `27-52`) — no callbacks, no `Option<T>` across FFI, no glyphon/cosmic-text/fontdb types crossing the boundary (mirrors epic 020's typography-boundary rule). Explicit ordered fallback chains are deferred; v1 relies on cosmic-text's automatic script fallback.

### Rust: `akar-core`

New module `crates/akar-core/src/font_source.rs` (or inline in `text_pipeline.rs` if the team prefers fewer files — precedent for small pipeline-adjacent types living in `text_pipeline.rs` already exists):

```rust
use thiserror::Error;

#[derive(Debug, Error)]
pub enum FontLoadError {
    #[error("font data could not be parsed: {0}")]
    InvalidFontData(String),
    #[error("no faces found in font data")]
    EmptyFontSource,
    /// v1 restricts loading to sources containing exactly one distinct family
    /// (Decisions — 2026-08-14, audit item 1). A .ttc/.otc spanning more than
    /// one family hits this; a single .ttf/.otf never does.
    #[error("font data contains {0} distinct families; v1 requires exactly one")]
    MultipleFamilies(usize),
}

/// How TextPipeline populates its fontdb::Database at construction time.
/// Default is Bundled — see Task 2 recommendation for why system scanning
/// is opt-in rather than automatic.
#[derive(Clone, Debug, Default)]
pub enum FontSource {
    /// Load only the given font bytes (TTF/OTF/TTC/OTC). Deterministic,
    /// reproducible across machines. WARNING (verified 2026-08-12): an
    /// empty database is NOT safe — cosmic-text panics with "no default
    /// font found" (cosmic-text-0.18.2/src/shape.rs:315) on the first
    /// shaping call. At least one font must always be present. Missing
    /// *coverage* within a present font does degrade gracefully to tofu.
    #[default]
    Bundled,
    /// Bundled bytes plus a full system font scan (fontdb::Database::load_system_fonts).
    /// Not reproducible across machines/OSes — see Task 2. Opt-in only.
    BundledPlusSystemScan,
}

pub struct TextPipelineConfig {
    pub font_source: FontSource,
    /// Font file bytes to load before any text is shaped. Each entry may
    /// contain multiple faces (TTC/OTC) — all are loaded.
    pub fonts: Vec<Vec<u8>>,
}
```

`TextPipeline::new` (`text_pipeline.rs:36-59`) gains a config parameter — proposed as `TextPipeline::new(device, queue, surface_format, config: TextPipelineConfig)`, with a `TextPipeline::new_default(device, queue, surface_format)` thin wrapper calling `TextPipelineConfig::default()` (empty `fonts`, `FontSource::Bundled`) to avoid a breaking one-line change at every existing call site being silent — `AkarCore::new` (`context.rs:24-42`) and its `mock()` path (`context.rs:48-60`) both need updating to thread this through, and `akar_ctx_new`/`akar_ctx_mock` (`akar-c-api/src/lib.rs:368-419`) need a config parameter or a builder (see C ABI below).

Construction changes at `text_pipeline.rs:41` from:
```rust
let font_system = glyphon::FontSystem::new();
```
to building a `fontdb::Database` explicitly (no `FontSystem::new()`/`new_with_fonts()` call, since both hard-code `load_system_fonts()` unconditionally — Research point 3):
```rust
let mut db = glyphon::fontdb::Database::new();
if matches!(config.font_source, FontSource::BundledPlusSystemScan) {
    db.load_system_fonts();
}
for bytes in &config.fonts {
    db.load_font_data(bytes.clone());
}
let font_system = glyphon::FontSystem::new_with_locale_and_db("en-US".to_string(), db);
```
(**Resolved 2026-08-12** — the snippet above previously called `sys_locale::get_locale()`. `sys-locale` is not a direct akar dependency; it is transitive via cosmic-text's `std` feature (`Cargo.lock:2149-2152`), and cosmic-text's own `FontSystem::get_locale()` is private (`cosmic-text-0.18.2/src/font/system.rs:411-417`). The locale only feeds `Fallback::script_fallback(script, locale)` — per-script fallback preference, mostly Han unification. Hardcoding `"en-US"` matches cosmic-text's own failure fallback, adds no dependency, and removes one more source of machine-dependence. Add a `locale: Option<String>` config field later if a CJK-language-correct fallback is needed.)

New method on `TextPipeline`: `pub fn load_font_bytes(&mut self, data: Vec<u8>) -> Result<(), FontLoadError>` for loading fonts after construction (uses `FontSystem::db_mut()`, `text_pipeline.rs` — note `db_mut()` clears the internal font-match cache on access per `cosmic-text-0.18.2/src/font/system.rs:280-284`, so no separate cache invalidation is needed).

### Rust: `akar-components` (epic 020 integration)

Extend `FontFamily` (`crates/akar-components/src/text_style.rs:3-8`) with a fourth, string-carrying variant:
```rust
#[derive(Clone, Debug, Default, PartialEq)]
pub enum FontFamily {
    #[default]
    SansSerif,
    Serif,
    Monospace,
    Named(String),
}
```
Note this drops `Copy`/`repr(C)` from the Rust-side `FontFamily` (a `String` cannot be `Copy`, and a data-carrying variant cannot be a plain C-style `#[repr(C)]` enum) — `Eq` is unaffected since `String` itself implements `Eq`, so it can still be derived. The existing `#[repr(C)]` enum shape stays reserved for the C ABI's numeric-tag encoding (see below); the Rust and C representations already diverge in spirit (`crates/akar-c-api/src/lib.rs:29-52` hand-converts between them), so this is a continuation of an existing pattern, not a new one. `resolved_to_attrs` (`text_style.rs:104-117`) gains a fifth match arm:
```rust
FontFamily::Named(name) => glyphon::Family::Name(name),
```
Fallback chain: add `fallback_families: Vec<String>` to `TextStyle`/`ResolvedTextStyle` (`text_style.rs:30-38`, `56-64`) defaulting to empty (inherits whatever `TextPipelineConfig::fallback_families` was set globally); when non-empty, akar would need to either (a) rely on cosmic-text's own per-script/monospace fallback machinery (`per_script_monospace_font_ids`, `Fallbacks`/`PlatformFallback` in `system.rs`) for codepoint-level fallback, which is automatic and not family-string-driven, or (b) implement explicit family-chain-try-in-order logic in akar by shaping against each family in turn and picking the first with full codepoint coverage. Recommend (a) for v1 — it's already what cosmic-text does internally for any text today, requires zero new akar code, and only the *initial* family (not the fallback chain) needs to be akar-configurable via `Named`. Explicit string-directed fallback chains (a genuine akar-level feature, not just "let cosmic-text's automatic fallback run") are proposed as a deferred v2 item — see Explicit Deferrals.

### C ABI: `akar-c-api`

New opaque-free, flat functions in `crates/akar-c-api/src/lib.rs`, following the existing `akar_ctx_new`/`akar_set_*` naming and the sentinel-for-`Option` convention (`SENTINEL_F32`/`SENTINEL_U32`, lines 12-14):

```c
// Load font bytes into the context's font database. Safe to call any time
// after akar_ctx_new/akar_ctx_mock, before or between frames. Returns 0 on
// success, non-zero on parse failure (mirrors existing akar error-code style
// — check other akar_* return codes for the established convention before
// implementing).
uint32_t akar_load_font_bytes(AkarCtx* ctx, const uint8_t* bytes, size_t len);

// Register a bundled font-loading source at context-creation configuration
// time. AKAR_FONT_SOURCE_BUNDLED (0, default) disables system font scanning
// entirely (reproducible). AKAR_FONT_SOURCE_BUNDLED_PLUS_SYSTEM_SCAN (1)
// additionally scans installed system fonts (Research: non-reproducible
// across machines — opt-in only).
typedef uint32_t AkarFontSource;
#define AKAR_FONT_SOURCE_BUNDLED 0
#define AKAR_FONT_SOURCE_BUNDLED_PLUS_SYSTEM_SCAN 1

// akar_ctx_new gains a trailing font_source parameter (breaking C signature
// change — acceptable per DEVELOP.md: "No stable public API exists yet").
AkarCtx* akar_ctx_new(const void* device, const void* queue,
                       uint32_t surface_format, AkarFontSource font_source);

```

`AkarFontFamily` (`crates/akar-c-api/src/lib.rs:29-32`) is today a plain `#[repr(C)] struct AkarFontFamily { pub value: u32 }` — a numeric-tag wrapper, not a native C enum with named variants; the `SansSerif`/`Serif`/`Monospace` mapping exists only as `0`/`1`/`2` decoded in Rust (`lib.rs:216-223`). Adding a fourth "named family" case doesn't fit cleanly into a bare `u32` tag plus a string, since C has no tagged-union sugar to attach a `const char*` to one specific tag value. Proposed: keep `AkarFontFamily`'s numeric tag exactly as-is for the three generics, and add a **separate** function for named families rather than overloading the tag — mirrors how C APIs typically handle "enum + escape hatch" (cf. `sokol_gfx.h` patterns referenced in `DEVELOP.md`'s UI/API design references):
```c
// Sets an explicit font family by name on a TextStyle-equivalent C struct,
// overriding the generic AkarFontFamily tag for this style only. Pass NULL
// to clear and revert to the generic tag.
void akar_text_style_set_family_name(AkarTextStyle* style, const char* name);
```
(Exact struct/field name TBD against whatever the current C `TextStyle`-equivalent struct is called in `akar-c-api/src/lib.rs` at implementation time — grep for the struct around lines 40-52 before writing this function, since this proposal is written from static reading, not a live compiler check of the exact struct name.)

## Explicit Deferrals

Out of scope for the first implementation of this epic (record kept here per Task 4's instruction):

- **Variable font axis control** (weight/width/optical-size interpolation via `fvar`) — carried over from the epic's original "Notes for Future Work"; requires API surface this proposal does not attempt to design.
- **Font subsetting** for binary-size-sensitive embedders — a later optimization once bundled-font-by-default (Task 2 recommendation) ships and binary size becomes a measured concern, not before.
- **Color/emoji font presentation** (COLR/CPAL, CBDT/CBLC, emoji ZWJ sequence → single-glyph presentation selection) — closely coupled to Unicode emoji-sequence handling in text editing (epic 018); scoping this into font-support risks scope creep into a dedicated emoji-support epic, per the original epic notes.
- **Explicit, akar-implemented string-directed fallback *chains*** (as opposed to relying on cosmic-text's automatic per-codepoint fallback, which ships "for free" with any font loading) — deferred to v2 per the "Rust: akar-components" section above; v1 exposes only the *initial* family choice (`FontFamily::Named`) plus whatever cosmic-text does automatically underneath.
- **Filesystem-path-based font loading** (`akar_load_font_file(ctx, path)`) — the epic's own Research called out path-based loading as "harder to keep portable" than bytes; v1 is bytes-only. A path-based convenience wrapper can be added later as a pure addition (load bytes from disk, call the bytes API) without breaking the bytes-based core.
- **Platform font APIs** (e.g. macOS `CTFontManager`, Windows `DirectWrite` enumeration beyond what `fontdb::load_system_fonts()` already does) — `fontdb`'s directory-scan approach is the only system-font mechanism proposed; no akar-specific platform font code.
- ~~**Live re-verification of Task 2/3 findings via actual builds/screenshots**~~ — **no longer deferred: done on 2026-08-12** at the shaping level (see the review section: `cargo test --workspace` passes, `demo-rust --screenshot` works, and the font-resolution/fallback/panic behavior was measured directly). What genuinely remains deferred is *cross-machine* pixel comparison, which needs a second machine and the CI visual-regression gate that `epics/014` punts; Task 6 and Task 8 carry the single-machine verification steps in the meantime.

---

## Tasks for Implementation (to be picked up by a coding agent)

These are concrete enough to hand to an implementation agent. They assume Tasks 1-4 above (research) are the starting context and should be read first.

### Task 5 — Add `FontSource`/`FontLoadError`/`TextPipelineConfig` to `akar-core` and thread through construction

**Status:** Done — landed 2026-08-14 together with Task 6.
**Readiness:** Implemented. What actually landed: `crates/akar-core/src/font_source.rs` defines `FontLoadError` (`InvalidFontData`/`EmptyFontSource`/`MultipleFamilies`), `FontSource` (`Bundled` default, `BundledPlusSystemScan`), `TextPipelineConfig { font_source, fonts }` with `Default` seeding the bundled faces, plus `bundled_fonts()`, `TextPipelineConfig::bundled()` and `::bundled_plus_system_scan()` helpers; all re-exported from `akar-core/src/lib.rs`. `TextPipeline::new` now takes a `TextPipelineConfig`, builds a `fontdb::Database` directly (system scan only under `BundledPlusSystemScan`), loads each `fonts` entry via `Source::Binary`, asserts the database is non-empty with an akar-side message, and constructs `FontSystem::new_with_locale_and_db("en-US", db)`; `TextPipeline::new_default` is the thin wrapper. `TextPipeline::load_font_bytes(Vec<u8>) -> Result<u32, FontLoadError>` uses `db_mut().load_font_source(...)`, collects distinct families across the returned `FaceInfo`s, registers exactly-one and returns its handle (repeat loads of the same family reuse the handle), and removes the inserted faces again for the zero-family and multi-family error paths. `TextPipeline::family_name(handle)` resolves a handle back to its family name (the lookup Task 7 will consume). `AkarCore::new` gained a `TextPipelineConfig` parameter; `AkarCore::mock()` passes `TextPipelineConfig::default()`, so it is never empty. `akar_ctx_new`/`akar_ctx_mock` keep their current C signatures and pass the default config — the `AkarFontSource` parameter is still Task 8. Deviation from the written proposal: `load_font_bytes` returns the family handle rather than `()`, per the "Decisions — 2026-08-14" item 1 wording ("register it and return its handle"); the `Result<(), _>` in the older "Proposed API" snippet is superseded.

- Add the types from "Proposed API" → "Rust: `akar-core`" above (`FontSource`, `FontLoadError` via `thiserror`, `TextPipelineConfig`) to a new `crates/akar-core/src/font_source.rs`, exported from `crates/akar-core/src/lib.rs` alongside the other public re-exports.
- Change `TextPipeline::new` (`crates/akar-core/src/text_pipeline.rs:36-59`) to accept a `TextPipelineConfig` and stop calling `glyphon::FontSystem::new()`; build a `fontdb::Database` directly per the "Construction changes" snippet in the Proposed API section, gating `db.load_system_fonts()` behind `FontSource::BundledPlusSystemScan`.
- Update the internal test helper `create_pipeline()` (`text_pipeline.rs:400-412`) to pass a `TextPipelineConfig` to the new `TextPipeline::new` signature so it keeps compiling. Note `shaped_buffer` (`text_pipeline.rs:385-398`, used by the tests at lines 432/446/459) is a separate, standalone helper that builds its own `glyphon::FontSystem`/`Buffer` directly — it never calls `TextPipeline::new` and does not need to change for the signature update to compile. It may still need attention once Task 6 bundles a real default font: `shaped_buffer` computes "expected" glyph geometry against a plain `glyphon::FontSystem::new()` (full system scan), while `TextPipeline` will compute "actual" geometry against the bundled-only `FontSource::Bundled` database — if those two resolve to different font files, the expected/actual comparisons in these tests could diverge. Check this when Task 6 lands, not as part of this task.
- Add `TextPipeline::load_font_bytes` using `FontSystem::db_mut().load_font_source(glyphon::fontdb::Source::Binary(...))`, **not** `load_font_data`: only `load_font_source` returns inserted IDs, which are required to detect invalid data and enumerate collection faces. Per "Decisions — 2026-08-14" (audit item 1): collect the distinct family names across all returned `FaceInfo`; zero families is `FontLoadError::EmptyFontSource`, exactly one registers and returns its handle, more than one is `FontLoadError::MultipleFamilies(count)` with nothing registered. `db_mut()` clears the font-match cache automatically, so no extra cache invalidation is needed.
- Update `AkarCore::new` (`crates/akar-core/src/context.rs:24-42`) to accept and forward a `TextPipelineConfig`; keep `AkarCore::mock()` (`context.rs:48-60`) on the populated bundled-default configuration from Task 6. It must never construct an empty database: existing layout/component tests require real shaped geometry, and cosmic-text panics on first shaping when no face exists.
- **Corrected 2026-08-12 — do not write the originally-proposed test.** The original text asked for a unit test asserting that `FontSource::Bundled` with zero fonts behaves "non-panicking, zero-size-or-tofu". That is factually wrong: zero fonts panics at `cosmic-text-0.18.2/src/shape.rs:315`. Write instead: (a) a test that a `TextPipeline` built from a config carrying at least one real font measures non-zero width for Latin text, and (b) a test that text whose script the loaded font does not cover measures non-zero but renders tofu rather than panicking (verified behavior: 4 glyphs, width `30.21` for `"你好世界"` against a Latin-only database). If a zero-font configuration must remain expressible, make it either impossible by construction (`TextPipelineConfig` always seeds the bundled default) or an explicit documented panic.
- **Confirmed risk, not hypothetical:** `crates/akar-layout/tests/text_measurement.rs` builds cores with `AkarCore::mock()` (`text_measurement.rs:12-14`) and asserts `size.width > 0.0` / `size.height > 0.0` (`text_measurement.rs:48-68`) plus wrap-height comparisons; `text_pipeline.rs:467-541` does the same through `create_pipeline()`. Both break the moment `mock()` gets an empty database. Keep `mock()` on a populated database at all times.

### Task 6 — Load a real bundled default font and make it the out-of-the-box default

**Status:** Done — landed 2026-08-14 in the same change as Task 5.
**Readiness:** Implemented. What actually landed: `IBMPlexSans-Regular.ttf` and `IBMPlexSans-SemiBold.ttf` are committed under `crates/akar-core/assets/fonts/` with the SIL OFL 1.1 text alongside them as `OFL.txt` (copied from `~/Projects/zed/assets/fonts/ibm-plex-sans/license.txt`). Both are embedded with `include_bytes!` in `font_source.rs` behind the default-on `bundled-font` feature declared in `crates/akar-core/Cargo.toml` (`default = ["bundled-font"]`), exposed as `IBM_PLEX_SANS_REGULAR`/`IBM_PLEX_SANS_SEMIBOLD` and gathered by `bundled_fonts()`, which `TextPipelineConfig::default()` uses. With the feature off, `bundled_fonts()` returns empty and `TextPipeline::new` fails the non-empty-database assertion with an explicit akar-side message naming the "caller must supply a font" requirement instead of cosmic-text's opaque `no default font found`. A new top-level `NOTICE` records the MIT/OFL split and points at the font files and `OFL.txt`; `LICENSE` is unchanged. `cargo build -p akar-core --no-default-features` compiles. Verified visually with `cargo run --bin demo-rust -- --screenshot /tmp/font-after.png --delay 0.5 --exit`: text renders in IBM Plex Sans across the whole UI, and `akar-diff` against a pre-change baseline shows the expected whole-UI text difference. One existing test moved: `text_pipeline::tests::measure_with_metadata_wrap_increases_height` asserted `narrow.width <= 40.5`, which no longer holds because IBM Plex Sans's widest unbreakable word in the sample string exceeds the 40px wrap constraint; the assertion is now `narrow.width < wide.width`, which is what the test actually means. `shaped_buffer` (the standalone test helper) still builds its own system-scanning `glyphon::FontSystem`; it only compares geometry against itself, so it was left alone.

Depends on Task 5 (`TextPipelineConfig`/`FontSource` and the config-driven construction path must exist before a default font can be loaded through it).

- **Decided 2026-08-12, amended 2026-08-14 (see "Decisions — 2026-08-14", items 3 and 4):** bundle **IBM Plex Sans Regular *and* SemiBold**, SIL OFL 1.1, committed to the repo behind a default-on `bundled-font` Cargo feature. Regular alone is insufficient — `heading.rs:34` requests `FontWeight::Bold` and a single-face database would render it at regular weight. Do not bundle the two italic faces; akar has no italic axis to select them with. Source files and license text are all available locally at `~/Projects/zed/assets/fonts/ibm-plex-sans/{IBMPlexSans-Regular.ttf,IBMPlexSans-SemiBold.ttf,license.txt}`; the license text was read in full during the verification pass. OFL 1.1 permits embedding and redistribution with any software provided the notice and license travel with it (`license.txt:48-51`, `53-61`), forbids selling the font by itself, forbids reusing the reserved name "Plex" for modified versions, and requires the font stay under OFL — none of which conflicts with akar's MIT `LICENSE`, which governs akar's code, not the font binary. Alternative verified locally: Lilex (OFL 1.1, `~/Projects/zed/assets/fonts/lilex/OFL.txt`) if a monospace default is also wanted. Avoid egui's `Ubuntu-Light.ttf` (Ubuntu Font Licence) and `Hack-Regular.ttf` (bespoke licence) — more restrictive and less standard. Caveat: these license texts are the copies distributed in those local checkouts and were not cross-checked upstream.
- **Asset location decided:** no font asset convention exists in this repo (no `*.ttf`/`*.otf`/`*.ttc`/`*.woff*` file exists anywhere outside `target/`). Create `crates/akar-core/assets/fonts/` — `include_bytes!` must resolve relative to the embedding crate — and put the OFL text next to the two font files. Add a `NOTICE` or a bundled-font paragraph in `LICENSE` recording the OFL obligation. Load via `include_bytes!` into the default `TextPipelineConfig::fonts` used by `AkarCore::new`'s default path and by `AkarCore::mock()`. Both `include_bytes!` calls sit behind `#[cfg(feature = "bundled-font")]` (default-on per decision 4); with the feature off, `TextPipelineConfig::default()` yields no fonts and the caller must supply at least one — emit a clear akar-side panic or error naming that requirement rather than letting cosmic-text's `no default font found` surface. Do **not** gitignore the font files: `include_bytes!` resolves at compile time, so an ignored asset breaks `cargo build` on a fresh clone.
- Verify via `cargo run --bin demo-rust -- --screenshot /tmp/font-default.png --exit` that text still renders (visual sanity check, not a diff yet — no baseline exists). This command was confirmed working on this machine on 2026-08-12, so it is a real gate, not an aspirational one. Capture a "before" screenshot on the current `main` first, then `akar-diff --diff` the two: expect a visible, whole-UI text difference, because default text currently resolves to `.SFNS-Regular` on macOS and will resolve to IBM Plex Sans after this change (measured `"Hello world"` widths: `74.68` vs `82.13` at 16px, so layout shifts too). Also re-run `cargo test --workspace` — the 354 tests currently pass, and the text-measurement assertions in `crates/akar-layout/tests/text_measurement.rs` are the ones most likely to move.
- Update `DEVELOP.md`'s "Local Dependencies"/font mentions if warranted, and note the bundled font choice in `README.md`'s Stack table if that table gets a "Fonts" row (optional, check with maintainers/existing PR conventions first).

### Task 7 — `FontFamily::Named` and C ABI `akar_load_font_bytes` / named-family selection

**Status:** Done — landed 2026-08-14.
**Readiness:** Implemented. What actually landed: `akar-core` gained the owned, `Copy` selection request in `font_source.rs` — `FontSelection { SansSerif, Serif, Monospace, Named(u32) }` and `FontRequest { family, weight: u16 }` (weight is the CSS numeric value, so no glyphon type crosses the boundary), both re-exported from `akar-core/src/lib.rs`. `TextPipeline::set_text_styled(buffer_id, text, metrics, width, height, FontRequest)` is the new entry point: it resolves a `Named` handle against its own `font_families` registry and builds the short-lived `glyphon::Attrs` internally, immediately before `Buffer::set_text`. The registry lookup and the `&mut` shaping call coexist by borrowing disjoint fields inside one function, with the shared body factored into a private free function `set_text_impl(&mut FontSystem, &mut buffers, &mut next_id, …)`; the pre-existing `TextPipeline::set_text(.., Option<Attrs>)` is retained unchanged for the other call sites and now delegates to the same helper. An unknown handle falls back to the generic sans-serif family rather than panicking. `akar-components`'s `FontFamily` gained `Named(u32)` and keeps `Copy`/`Eq`/`repr(C)` (as do `TextStyle` and `ResolvedTextStyle`); `resolved_to_attrs` was **replaced** by `resolved_to_font_request(&ResolvedTextStyle) -> akar_core::FontRequest`, and `heading.rs`, `link.rs`, `paragraph.rs` all now call `set_text_styled`. No component borrows a registry name and no `Attrs` value crosses back out of `akar-core`. On the C side: `akar_load_font_bytes(ctx, bytes, len, out_handle) -> uint32_t` returns `AKAR_FONT_LOAD_OK` (0) / `AKAR_FONT_LOAD_INVALID_ARGUMENT` (1) / `AKAR_FONT_LOAD_INVALID_DATA` (2) / `AKAR_FONT_LOAD_EMPTY_SOURCE` (3) / `AKAR_FONT_LOAD_MULTIPLE_FAMILIES` (4), writing the handle only on success and leaving `out_handle` untouched otherwise (NULL `out_handle` is allowed); `AkarTextStyle` gained `font_family_name_handle: u32` with the dedicated `AKAR_FONT_FAMILY_NAME_HANDLE_NONE` (`UINT32_MAX`) sentinel, decoded in `c_text_style_to_rust` after the generic tag so it overrides it. No setter function was added. `akar.h` was regenerated by `crates/akar-c-api/build.rs` (not hand-edited). Deviations from the written bullets, both forced: (a) the C function takes a `uint32_t len` and an out-parameter rather than the proposal's `size_t len` with a bare handle return — `u32` length matches the surrounding convention (`akar_push_paste`), and a single `u32` return cannot carry both a status code and a handle; (b) the third C test verifies the named handle round-trips through the flat `AkarTextStyle` field into a successful `akar_paragraph`/`akar_heading` paint, since `resolved_to_attrs` no longer exists and nothing internal is observable from C — the "handle maps to the right family" assertions live in Rust instead (`c_text_style_name_handle_overrides_generic_family` in `akar-c-api`, `resolved_to_font_request`/`named_family_*` in `akar-components`, `set_text_styled_named_handle_shapes_text` and `set_text_styled_unknown_handle_falls_back_to_sans_serif` in `akar-core`). Verified: `cargo fmt`, clippy clean on the touched files (the 5 pre-existing `akar-components` lints and 1 pre-existing `akar-core` lint are untouched), `cargo test --workspace` 370 passing (361 before, 9 added), and `demo-rust --screenshot` plus `--component heading/paragraph/link` all still render text correctly.

Depends on Task 5 (`TextPipeline::load_font_bytes` must exist before `akar_load_font_bytes` has anything to call). Independent of Task 6 — does not require a bundled default font to land first.

- Extend `crates/akar-components/src/text_style.rs`'s `FontFamily` enum (`text_style.rs:3-8`) with `Named(u32)`. Convert resolved styles to an owned akar-core font-selection request (generic family or handle, plus weight) and pass it into the new `TextPipeline` entry point from "Decisions — 2026-08-14" (audit item 2), which resolves the handle and constructs `glyphon::Attrs` internally immediately before `Buffer::set_text`. Migrate the generic-family call sites (`heading.rs`, `link.rs`, `paragraph.rs`) to the same entry point. Do not pass a registry into `resolved_to_attrs` or return `Attrs` borrowing a registry name. With the handle design, `FontFamily`, `TextStyle` and `ResolvedTextStyle` keep `Copy`/`Eq`/`repr(C)`.
- Add the finalized collection-aware `akar_load_font_bytes` API to `crates/akar-c-api/src/lib.rs`, returning a distinct error code for `MultipleFamilies` alongside the existing invalid-data/empty-source codes (mirror akar's established `akar_*` return-code convention), and add a `font_family_name_handle: u32` field to `AkarTextStyle`. Use a dedicated `UINT32_MAX`/`u32::MAX` sentinel for this handle field, not the existing `0xFF` small-enum sentinel. Do not add a setter function. Follow the surrounding null-pointer patterns.
- Add C ABI integration tests under `crates/akar-c-api/tests/` (per `AGENTS.md` → "Testing approach": "C ABI tests are written in C and compiled as integration tests") covering: loading valid font bytes succeeds, loading garbage bytes fails cleanly (no panic, no crash), and a `Named` family round-trips through `resolved_to_attrs` to the correct `glyphon::Family::Name`. Existing pattern to follow: `crates/akar-c-api/tests/text_edit.c` (uses `akar_ctx_mock()` at line 39), compiled by `crates/akar-c-api/build.rs` via the `cc` crate — a new `.c` file needs a corresponding `cc::Build` entry in that build script.

### Task 8 — `akar_ctx_new` font-source parameter and default-off system scanning

**Status:** Not Started
**Readiness:** Dependency-blocked on Tasks 5+6; otherwise ready. The call-site and header-generation checks are complete.

Depends on Task 5 (`FontSource`/`TextPipelineConfig` must exist on the Rust side before the C enum and `akar_ctx_new` parameter can forward to them). Independent of Tasks 6/7 — does not require the bundled default font or `FontFamily::Named` to land first.

- Add the `AkarFontSource` C enum/`#define` constants and thread a `font_source` parameter through `akar_ctx_new` (`crates/akar-c-api/src/lib.rs:368-395`) and `akar_ctx_mock` (`lib.rs:407-419`) per the Proposed API's C ABI section. This is a breaking C signature change — acceptable per `DEVELOP.md`'s "Project Status" ("No stable public API exists yet"). No caller updates are required: verified 2026-08-12 that nothing in the repo calls `akar_ctx_new`. Keep `akar_ctx_mock()` argument-free and pinned to the bundled default, so the 122 `AkarCore::mock()`-based tests are unaffected.
- Regenerate `akar.h` by building (`crates/akar-c-api/build.rs` runs `cbindgen::generate` and writes the workspace-root header automatically); commit the result. Do not hand-edit `akar.h` — see `AGENTS.md` "What NOT to do".
- Add a regression test/check (can be a `cargo test` in `akar-c-api` or a documented manual step) that constructing two `AkarCtx` on the default `AKAR_FONT_SOURCE_BUNDLED` path with the same input text produces byte-identical `--dump-frame` glyph output across repeated runs on the same machine, as a baseline for the eventual cross-machine reproducibility check called out in the Explicit Deferrals section.

### Task 9 — Re-attempt Task 3 (CJK/Arabic prototype) using the new public API

**Status:** Not Started
**Readiness:** Dependency-blocked on Tasks 5-7. Its test plan is otherwise ready, but external redistributable CJK/Arabic test assets remain intentionally uncommitted; local system fonts may be used for the prototype only.

Depends on Tasks 5, 6, and 7 (needs `load_font_bytes`/`akar_load_font_bytes` and `FontFamily::Named` to reference a loaded family by name); Task 8 is not a hard dependency (system-scan opt-in is orthogonal to loading explicit bytes) but should land first if practical, since it's the more architecturally central C ABI change and this task is the epic's final verification step regardless of order.

- Once Tasks 5-8 land, load a CJK font (e.g. a Noto Sans CJK subset) and an Arabic font (e.g. Noto Naskh Arabic) via the new `akar_load_font_bytes`/`TextPipeline::load_font_bytes`, reference them via `FontFamily::Named`, and render sample CJK and Arabic strings through `demo-rust` (a small script under `examples/demo-rust/scripts/` following the pattern of `text_edit_*.txt`, or a dedicated demo panel).
- Capture screenshots via `--screenshot`/`--component` and visually confirm glyph rendering (correct CJK glyphs, correctly shaped/joined Arabic letterforms) — this is the actual empirical version of the original Task 3, now unblocked by having a supported loading path instead of a bypass spike. Expected baseline from the 2026-08-12 probe (shaping only, not pixels): Arabic shapes with every glyph at an RTL bidi level and CJK shapes 1:1 codepoint-to-glyph, provided a covering font is loaded; an uncovered script yields tofu, not a panic.
- Record any shaping/coverage failures found as follow-up tasks in epic 023 (RTL) rather than re-opening this epic, per this epic's "soft prerequisite" framing.

---

## Notes for Future Work

- Variable font axis control (weight/width interpolation) is out of scope until core loading/fallback lands.
- Font subsetting for binary-size-sensitive embedders is a later optimization, not a blocker.
- Color/emoji font presentation is closely related to Unicode emoji-sequence handling in text editing (epic 018) and should be scoped carefully to avoid scope creep into a full emoji-support epic.
- (Superseded/expanded by the "Explicit Deferrals" section above, which is the authoritative deferral list as of this research pass — kept here unchanged for history.)
